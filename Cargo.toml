[workspace]
members = [
    "crates/rssp-core",
    "crates/rssp",
    "crates/rssp-cli",
]
resolver = "3"

[workspace.package]
version = "0.4.248"

[workspace.dependencies]
rssp = { path = "crates/rssp" }
rssp-core = { path = "crates/rssp-core"}

[profile.release]
lto = "fat"
codegen-units = 1
panic = "abort"
strip = true

[profile.bench]
debug = true
