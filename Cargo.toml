[workspace]
members = [
    "crates/rssp-core",
    "crates/rssp",
    "crates/rssp-cli",
]
resolver = "3"

[workspace.package]
version = "0.4.105"

[workspace.dependencies]
rssp = { path = "crates/rssp", version = "0.4.102" }
rssp-core = { path = "crates/rssp-core", version = "0.4.102" }

[profile.release]
lto = "fat"
codegen-units = 1
panic = "abort"
strip = true

[profile.bench]
debug = true
